version: '2'

services:

  prophet:
    #image: gitlab.ow2.org:4567/melodic/morphemic-preprocessor/prophet:morphemic-rc1.5
    image: activeeon/morphemic
    network_mode: bridge
    container_name: prophet
    env_file:
      - variables.env
    volumes:
      - /home/diana/Desktop/Prophet:/tmp/Notebooks

      
  gluonts:
    #image: gitlab.ow2.org:4567/melodic/morphemic-preprocessor/gluonts:morphemic-rc1.5
    image: activeeon/morphemic
    network_mode: bridge
    container_name: gluonts
    env_file:
      - variables.env
    volumes:
      - /home/diana/Desktop/GluonTS:/tmp/Notebooks

  #prometheus:
  #  image: prom/prometheus
  #  ports:
  #    - 9090:9090
  #  container_name: prometheus
  #  volumes:
  #  - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
